import 'package:flutter/material.dart';

class GlucoHistoryTap extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('home tap', style: TextStyle(fontSize: 24))
    );
  }
}